const UiController = () => {
  const msg = (type, info) => {
    return;
  };

  const displayShip = (player, positions) => {
    return;
  }

  return { msg };
};
const uiController = UiController();
export default uiController;
